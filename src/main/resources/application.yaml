quarkus:
  http:
    root-path: /api

  datasource:
    db-kind: postgresql
    username: russkiy-po-moyemu
    password: russkiy-po-moyemu
    jdbc:
      url: jdbc:postgresql://localhost:5432/russkiy-po-moyemu

  hibernate-orm:
    database:
      generation: drop-and-create

mp:
  jwt:
    verify:
      publickey:
        location: file:${PUBLIC_KEY_LOC}

smallrye:
  jwt:
    sign:
      key:
        location: file:${PRIVATE_KEY_LOC}